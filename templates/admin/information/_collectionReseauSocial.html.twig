{% block _information_reseaux_sociaux_widget %}
    {{ form_widget(form) }}
    <input type="hidden" id="widgets-reseau-social-counter" value="0">
    <button type="button" id="add-reseau-social" class="btn btn-primary">
        <i class="fas fa-plus"></i>
        Ajouter un réseau social
    </button>
{% endblock %}

{% block _information_reseaux_sociaux_entry_row %}
    {{ form_widget(form) }} 
{% endblock %}

{% block _information_reseaux_sociaux_entry_widget %}
    <div class="form-group" id="block_{{id}}">
        <div class="row">
            <div class="col-11 pr-2">
                {{ form_errors(form.nom) }}   
                {{ form_widget(form.nom) }}
                {{ form_errors(form.url) }}   
                {{ form_widget(form.url) }}
                {{ form_errors(form.icone) }}
                {{ form_widget(form.icone) }}
                <span class="input-group-append">
                    <button id="{{id}}" class="btn border"></button>
                </span>
            </div>
            <div class="col-1 d-flex p-0">
                <button type="button" data-action="delete" data-target="#block_{{id}}" class="btn btn-danger"><i class="fas fa-times"></i></button>
            </div>
        </div>
    </div>
{% endblock %}